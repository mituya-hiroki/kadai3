
<h2>Editing Book</h2>
<% if @book.errors.any? %>
	<h1><%= pluralize(@book.errors.count, "error") %>prohibited this obj from being saved:</h1>
	<%= @book.errors.full_messages.each do |message| %>
	<li><%= message %></li>
	<% end %>
<% end %>

<%= form_for(@book) do |f| %>
<h4>Title</h4>
<%= f.text_field :title %>
<h4>Opinion</h4>
<%= f.text_area :body %>

<%= f.submit "Update Book" %>
<br>	
<%= link_to "Show",book_path(@book.id) %>
<%= link_to "Back",books_path%>
<% end %>